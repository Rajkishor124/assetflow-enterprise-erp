spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:assetflow_dev}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

logging:
  level:
    com.assetflow: INFO
    org.springframework.web: INFO
